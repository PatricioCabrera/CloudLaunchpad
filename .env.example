# Copy to .env before running `docker compose up`.
# Local development only — these never reach AWS. In the deployed stack the
# database credential comes from Secrets Manager, injected into the ECS task.
DB_USER=cloudlaunchpad
DB_PASSWORD=change_me_locally
